<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Panel Admin' %> - Centrale Quanta</title>
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="https://res.cloudinary.com/dak7xowfm/image/upload/v1765661878/centrale_quanta_logo_mq9csz.jpg">
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body class="bg-gray-50 text-gray-800 font-sans">

    <div class="flex min-h-screen">
        <!-- Sidebar -->
        <aside class="w-64 bg-gray-800 text-white flex flex-col">
            <div class="px-6 py-4 border-b border-gray-700">
                <h1 class="text-xl font-bold tracking-wider">CENTRALE QUANTA</h1>
                <span class="text-sm text-gray-400">Panel d'Administration</span>
            </div>
            <nav class="flex-1 px-4 py-4 space-y-2">
                <a href="<%= adminPath %>" class="flex items-center px-4 py-2 rounded-lg hover:bg-gray-700">
                    <i class="fas fa-tachometer-alt w-6"></i>
                    <span>Tableau de bord</span>
                </a>
                <a href="<%= adminPath %>/posts" class="flex items-center px-4 py-2 rounded-lg hover:bg-gray-700">
                    <i class="fas fa-newspaper w-6"></i>
                    <span>Gérer les articles</span>
                </a>
                <a href="<%= adminPath %>/events" class="flex items-center px-4 py-2 rounded-lg hover:bg-gray-700">
                    <i class="fas fa-calendar-alt w-6"></i>
                    <span>Gérer les événements</span>
                </a>
                 <a href="<%= adminPath %>/members" class="flex items-center px-4 py-2 rounded-lg hover:bg-gray-700">
                    <i class="fas fa-users w-6"></i>
                    <span>Gérer l'équipe</span>
                </a>
                 <a href="<%= adminPath %>/sections" class="flex items-center px-4 py-2 rounded-lg hover:bg-gray-700">
                    <i class="fas fa-puzzle-piece w-6"></i>
                    <span>Gérer les sections</span>
                </a>
                 <hr class="border-gray-700 my-2">
                <a href="<%= adminPath %>/comments" class="flex items-center justify-between px-4 py-2 rounded-lg hover:bg-gray-700">
                    <div class="flex items-center">
                        <i class="fas fa-comments w-6"></i>
                        <span>Commentaires</span>
                    </div>
                    <% if (locals.unreadCommentsCount && unreadCommentsCount > 0) { %>
                        <span class="bg-red-500 text-white text-xs font-bold rounded-full h-6 w-6 flex items-center justify-center">
                            <%= unreadCommentsCount %>
                        </span>
                    <% } %>
                </a>
                <a href="<%= adminPath %>/messages" class="flex items-center justify-between px-4 py-2 rounded-lg hover:bg-gray-700">
                     <div class="flex items-center">
                        <i class="fas fa-envelope w-6"></i>
                        <span>Messages</span>
                    </div>
                    <% if (locals.unreadMessageCount && unreadMessageCount > 0) { %>
                        <span class="bg-red-500 text-white text-xs font-bold rounded-full h-6 w-6 flex items-center justify-center">
                            <%= unreadMessageCount %>
                        </span>
                    <% } %>
                </a>
            </nav>
            <div class="px-6 py-4 border-t border-gray-700">
                <a href="/" target="_blank" class="flex items-center text-sm text-gray-300 hover:text-white">
                    <i class="fas fa-globe w-6"></i>
                    <span>Voir le site</span>
                </a>
            </div>
        </aside>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col">
             <header class="bg-white shadow-sm p-4 flex justify-between items-center">
                <h2 class="text-xl font-semibold text-gray-700"><%= title %></h2>
                 <% if (user) { %>
                    <div class="flex items-center space-x-4">
                        <span class="text-gray-600">Bonjour, <%= user.username %></span>
                        <a href="<%= adminPath %>/logout" class="flex items-center text-sm font-medium text-red-600 hover:text-red-800">
                            <i class="fas fa-sign-out-alt w-5 mr-1"></i>
                            <span>Se déconnecter</span>
                        </a>
                    </div>
                <% } %>
            </header>
            <main class="flex-1 p-6 md:p-10">
                <%- body %>
            </main>
        </div>
    </div>

</body>
</html>
