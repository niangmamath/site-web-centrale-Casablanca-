<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Portail Admin - Centrale Quanta</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.95); }
      to { opacity: 1; transform: scale(1); }
    }
    .animate-fade-in {
      animation: fadeIn 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
    }
    .input-glow:focus-within {
      box-shadow: 0 0 15px 2px rgba(59, 130, 246, 0.4);
    }
  </style>
</head>
<body class="bg-gradient-to-br from-gray-900 via-blue-900 to-gray-900 min-h-screen flex items-center justify-center p-4 font-sans">

  <div class="w-full max-w-md animate-fade-in">
    
    <div class="text-center mb-8">
        <h1 class="text-4xl md:text-5xl font-extrabold text-white tracking-wide">
            Centrale <span class="text-blue-400">Quanta</span>
        </h1>
        <p class="text-blue-200 mt-2 text-lg">Portail d'Administration</p>
    </div>

    <div class="bg-gray-800 bg-opacity-50 backdrop-blur-sm p-8 rounded-2xl shadow-2xl border border-gray-700">
        
        <% if (error) { %>
          <div class="bg-red-500/20 border border-red-500/30 text-red-300 px-4 py-3 rounded-lg relative mb-6 flex items-center" role="alert">
            <i class="fas fa-exclamation-circle mr-3"></i>
            <span class="block sm:inline"><%= error %></span>
          </div>
        <% } %>

        <form action="<%= adminPath %>/login" method="POST" class="space-y-6">
          
          <div class="relative group input-glow transition-shadow duration-300 rounded-lg">
            <i class="fas fa-user absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 group-focus-within:text-blue-400 transition-colors duration-300"></i>
            <input type="text" id="username" name="username" required
                   class="w-full bg-gray-900/50 text-white placeholder-gray-400 border-2 border-gray-700 rounded-lg py-3 pl-12 pr-4 focus:outline-none focus:border-blue-500 transition-colors duration-300"
                   placeholder="Nom d'utilisateur">
          </div>
          
          <div class="relative group input-glow transition-shadow duration-300 rounded-lg">
            <i class="fas fa-lock absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 group-focus-within:text-blue-400 transition-colors duration-300"></i>
            <input type="password" id="password" name="password" required
                   class="w-full bg-gray-900/50 text-white placeholder-gray-400 border-2 border-gray-700 rounded-lg py-3 pl-12 pr-4 focus:outline-none focus:border-blue-500 transition-colors duration-300"
                   placeholder="Mot de passe">
          </div>
          
          <div>
            <button type="submit"
                    class="w-full bg-gradient-to-r from-blue-600 to-indigo-600 text-white font-bold py-3 px-4 rounded-lg shadow-lg hover:shadow-blue-500/50 transform hover:scale-105 transition-all duration-300 focus:outline-none focus:ring-4 focus:ring-blue-500/50">
              Se connecter
            </button>
          </div>
        </form>

        <div class="text-center mt-8">
            <a href="/" class="inline-block text-sm text-blue-300 align-baseline hover:text-white transition-colors duration-300">
                <i class="fas fa-arrow-left mr-2"></i>
                Retour au site principal
            </a>
        </div>
    </div>

  </div>
</body>
</html>
